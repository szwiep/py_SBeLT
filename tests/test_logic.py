import unittest
import random
import numpy as np

from ..model import logic

ATTR_COUNT = 6 # Number of attributes associated with a Particle

class TestGetEventParticles(unittest.TestCase()):
    def setUp(self):
        self.particle_array = np.zeros((10, ATTR_COUNT))
        prin

    def test_default_subregion():
        # create subregion object (just needs to be list of x objects)
        # define number of particles to entrain
        # define level limit
        print()
    
    def test_bad_length():
        print()

if __name__ == '__main__':
    unittest.main()